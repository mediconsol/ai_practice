# 토큰 관리 자동화 가이드 (Phase 4)

## 개요
토큰 사용량 관리의 자동화 기능을 구현했습니다. 월간 자동 리셋, 실시간 경고 배너, 그리고 관리 함수를 포함합니다.

---

## 구현된 기능

### 1. 월간 자동 토큰 리셋

#### 데이터베이스 함수
```sql
-- 모든 사용자의 토큰 리셋
SELECT reset_monthly_tokens();

-- 특정 사용자만 리셋 (테스트용)
SELECT manual_reset_tokens('user-uuid');

-- 모든 사용자 수동 리셋
SELECT manual_reset_tokens();
```

#### 자동 실행 (Supabase Cron)
```sql
-- 매월 1일 00:00 UTC에 자동 실행
SELECT cron.schedule(
  'monthly-token-reset',
  '0 0 1 * *',
  $$SELECT reset_monthly_tokens();$$
);
```

**참고**: Supabase에서 pg_cron 활성화가 필요합니다.
- Supabase Dashboard > Database > Extensions > pg_cron 활성화
- 또는 Supabase CLI: `supabase db extension pg_cron enable`

### 2. 토큰 경고 시스템

#### 경고 레벨
| 사용률 | 레벨 | 색상 | 메시지 |
|--------|------|------|--------|
| 100%+ | Critical | 빨강 | "토큰을 모두 사용했습니다" |
| 90-99% | High | 빨강 | "토큰이 거의 소진되었습니다" |
| 80-89% | Medium | 주황 | "토큰 사용량이 높습니다" |
| 70-79% | Low | 파랑 | "토큰 사용량 알림" |
| <70% | None | - | 배너 표시 안함 |

#### 경고 체크 함수
```sql
-- 사용자의 경고 레벨 확인
SELECT * FROM check_token_warning('user-uuid');

-- 반환 예시:
{
  "needs_warning": true,
  "warning_level": "high",
  "percentage": 92.5,
  "usage": 9250,
  "limit": 10000,
  "remaining": 750
}
```

### 3. 실시간 경고 배너

#### TokenWarningBanner 컴포넌트
**위치**: 모든 페이지 상단 (헤더 바로 아래)

**기능**:
- 70% 이상 사용 시 자동 표시
- 레벨별 색상 및 메시지
- 프로그레스 바 표시
- 액션 버튼 (업그레이드/상세 보기)
- 닫기 버튼 (Critical 제외)

**표시 예시**:

```
┌─────────────────────────────────────────────────────┐
│ ⚠️ 토큰이 거의 소진되었습니다                         │
│ 750 토큰만 남았습니다. 곧 AI 실행이 제한됩니다.       │
│ ████████████████████░░ 92%                          │
│                                  [업그레이드] [X]    │
└─────────────────────────────────────────────────────┘
```

---

## 파일 구조

### 생성된 파일
```
supabase/migrations/
└── 20251225000003_token_reset_automation.sql

src/components/layout/
└── TokenWarningBanner.tsx

docs/
└── TOKEN_AUTOMATION_GUIDE.md
```

### 수정된 파일
```
src/components/layout/
└── DashboardLayout.tsx (TokenWarningBanner 추가)
```

---

## 마이그레이션 실행

### 1. SQL 마이그레이션
Supabase Dashboard > SQL Editor에서 실행:

```bash
cat supabase/migrations/20251225000003_token_reset_automation.sql
```

### 2. pg_cron 활성화 (선택사항)

**방법 1: Supabase Dashboard**
1. Dashboard > Database > Extensions
2. pg_cron 검색
3. Enable 버튼 클릭

**방법 2: SQL**
```sql
CREATE EXTENSION IF NOT EXISTS pg_cron;
```

**방법 3: Supabase CLI**
```bash
supabase db extension pg_cron enable
```

### 3. Cron Job 등록 (pg_cron 활성화 후)

```sql
SELECT cron.schedule(
  'monthly-token-reset',
  '0 0 1 * *',
  $$SELECT reset_monthly_tokens();$$
);

-- Cron Job 확인
SELECT * FROM cron.job;

-- Cron Job 실행 내역 확인
SELECT * FROM cron.job_run_details ORDER BY start_time DESC LIMIT 10;
```

---

## 사용 방법

### 1. 자동 리셋 (프로덕션)

**설정 후 자동 실행**:
- 매월 1일 00:00 UTC에 자동으로 모든 사용자의 토큰 리셋
- 로그 확인: `cron.job_run_details` 테이블

### 2. 수동 리셋 (테스트/관리)

**특정 사용자 리셋**:
```sql
-- 사용자 ID 확인
SELECT id, email FROM profiles WHERE email = 'user@example.com';

-- 해당 사용자만 리셋
SELECT manual_reset_tokens('user-uuid');
```

**전체 사용자 리셋**:
```sql
SELECT manual_reset_tokens();
```

### 3. 경고 확인

**프론트엔드 자동 확인**:
- 30초마다 자동으로 토큰 사용량 조회 (`useTokenUsage` hook)
- 70% 이상 시 배너 자동 표시
- 실시간 프로그레스 바 업데이트

**수동 확인**:
```sql
SELECT * FROM check_token_warning('user-uuid');
```

---

## 테스트 시나리오

### 시나리오 1: 70% 경고 (Low)
```sql
-- 사용량을 70%로 설정
UPDATE profiles
SET tokens_used_this_month = 7000
WHERE email = 'test@example.com';

-- 결과: 파란색 배너 표시
-- "토큰 사용량 알림"
-- "이번 달 70% 사용 중입니다. 3,000 토큰 남음."
```

### 시나리오 2: 80% 경고 (Medium)
```sql
UPDATE profiles
SET tokens_used_this_month = 8000
WHERE email = 'test@example.com';

-- 결과: 주황색 배너 표시
-- "토큰 사용량이 높습니다"
-- "2,000 토큰 남음. Pro 플랜을 고려해보세요."
```

### 시나리오 3: 90% 경고 (High)
```sql
UPDATE profiles
SET tokens_used_this_month = 9000
WHERE email = 'test@example.com';

-- 결과: 빨간색 배너 표시
-- "토큰이 거의 소진되었습니다"
-- "1,000 토큰만 남았습니다. 곧 AI 실행이 제한됩니다."
```

### 시나리오 4: 100% 초과 (Critical)
```sql
UPDATE profiles
SET tokens_used_this_month = 10000
WHERE email = 'test@example.com';

-- 결과: 빨간색 배너 표시 (닫기 불가)
-- "토큰을 모두 사용했습니다"
-- "더 이상 AI를 실행할 수 없습니다. Pro 플랜으로 업그레이드하세요."
-- AI 실행 차단
```

### 시나리오 5: 월간 리셋 테스트
```sql
-- 현재 상태 확인
SELECT email, tokens_used_this_month, tokens_reset_at
FROM profiles
WHERE email = 'test@example.com';

-- 수동 리셋 실행
SELECT manual_reset_tokens();

-- 결과 확인
SELECT email, tokens_used_this_month, tokens_reset_at
FROM profiles
WHERE email = 'test@example.com';

-- 예상 결과:
-- tokens_used_this_month: 0
-- tokens_reset_at: 다음 달 1일
```

---

## UI/UX 흐름

### 1. 정상 사용 (70% 미만)
```
┌─────────────────┐
│ MediConSol      │ ← 헤더
├─────────────────┤
│                 │ ← 배너 없음
│ [대시보드 내용] │
│                 │
└─────────────────┘
```

### 2. 경고 표시 (70% 이상)
```
┌─────────────────────────────────┐
│ MediConSol                      │ ← 헤더
├─────────────────────────────────┤
│ ⚠️ 토큰 사용량 알림              │ ← 경고 배너
│ 70% 사용 중. 3,000 토큰 남음     │
│ ████████████░░░░ 70%            │
│              [상세보기] [X]     │
├─────────────────────────────────┤
│ [대시보드 내용]                  │
└─────────────────────────────────┘
```

### 3. 임계 상태 (100%)
```
┌─────────────────────────────────┐
│ MediConSol                      │ ← 헤더
├─────────────────────────────────┤
│ 🚨 토큰을 모두 사용했습니다      │ ← 경고 배너 (닫기 불가)
│ AI 실행 불가. 업그레이드 필요    │
│ ████████████████████ 100%       │
│              [지금 업그레이드]  │
├─────────────────────────────────┤
│ [대시보드 내용]                  │
└─────────────────────────────────┘
```

---

## 배너 동작

### 자동 표시
- 페이지 로드 시 자동 확인
- 30초마다 자동 갱신 (`useTokenUsage` hook)
- 70% 이상 시 즉시 표시

### 닫기 기능
- **70-89%**: 닫기 버튼 있음 (X)
- **90-99%**: 닫기 버튼 있음 (X)
- **100%+**: 닫기 불가 (업그레이드 필수)

### 세션 지속
- 닫은 후 새로고침 시 다시 표시
- localStorage 저장 없음 (항상 최신 상태)

---

## 모니터링

### 1. Cron Job 실행 확인
```sql
-- 최근 10회 실행 내역
SELECT
  jobid,
  runid,
  start_time,
  end_time,
  status,
  return_message
FROM cron.job_run_details
WHERE jobid = (SELECT jobid FROM cron.job WHERE jobname = 'monthly-token-reset')
ORDER BY start_time DESC
LIMIT 10;
```

### 2. 토큰 사용 통계
```sql
-- 전체 사용자 토큰 사용 현황
SELECT
  subscription_tier,
  COUNT(*) as user_count,
  AVG(tokens_used_this_month) as avg_usage,
  SUM(tokens_used_this_month) as total_usage
FROM profiles
GROUP BY subscription_tier;

-- 경고 대상 사용자 (70% 이상)
SELECT
  email,
  tokens_used_this_month,
  token_limit,
  ROUND((tokens_used_this_month::NUMERIC / token_limit::NUMERIC) * 100, 1) as percentage
FROM profiles
WHERE
  token_limit > 0 AND
  (tokens_used_this_month::NUMERIC / token_limit::NUMERIC) >= 0.7
ORDER BY percentage DESC;
```

### 3. 리셋 필요 사용자
```sql
-- 리셋 날짜가 지난 사용자
SELECT
  email,
  tokens_used_this_month,
  tokens_reset_at,
  NOW() - tokens_reset_at as overdue
FROM profiles
WHERE tokens_reset_at <= NOW();
```

---

## 트러블슈팅

### 문제 1: pg_cron이 활성화되지 않음
**증상**: Cron Job 등록 실패
**해결**:
```sql
-- Extension 확인
SELECT * FROM pg_available_extensions WHERE name = 'pg_cron';

-- 수동 활성화
CREATE EXTENSION IF NOT EXISTS pg_cron;
```

### 문제 2: Cron Job이 실행되지 않음
**증상**: 매월 1일에 리셋이 안됨
**해결**:
```sql
-- Job 상태 확인
SELECT * FROM cron.job WHERE jobname = 'monthly-token-reset';

-- 수동 실행 테스트
SELECT cron.unschedule('monthly-token-reset');
SELECT reset_monthly_tokens(); -- 수동 실행 확인

-- 다시 등록
SELECT cron.schedule(
  'monthly-token-reset',
  '0 0 1 * *',
  $$SELECT reset_monthly_tokens();$$
);
```

### 문제 3: 경고 배너가 표시되지 않음
**증상**: 70% 이상인데 배너 없음
**해결**:
1. 브라우저 캐시 삭제
2. React Query 캐시 확인
3. 토큰 사용량 직접 확인:
```sql
SELECT * FROM check_token_warning('user-uuid');
```

### 문제 4: 배너가 계속 표시됨
**증상**: 닫아도 다시 나타남
**해결**: 정상 동작입니다. 배너는 세션 간 지속되지 않고, 항상 최신 토큰 상태를 반영합니다.

---

## 다음 단계 (선택사항)

### 1. 이메일 알림
Supabase Edge Function으로 이메일 발송:
```typescript
// supabase/functions/send-token-warning/index.ts
import { Resend } from 'resend';

const resend = new Resend(Deno.env.get('RESEND_API_KEY'));

// 80%, 90%, 100% 도달 시 이메일 발송
```

### 2. 슬랙 알림
관리자에게 슬랙 알림:
```typescript
// Critical 사용자 발생 시 슬랙 알림
```

### 3. 관리자 대시보드
- 전체 사용자 토큰 사용 현황
- 리셋 필요 사용자 목록
- 월별 사용량 트렌드

---

## 체크리스트

### 구현 완료
- [x] 월간 토큰 리셋 함수
- [x] 수동 리셋 함수 (테스트용)
- [x] 경고 레벨 체크 함수
- [x] 토큰 경고 배너 컴포넌트
- [x] DashboardLayout에 배너 추가
- [x] 레벨별 색상 및 메시지
- [x] 프로그레스 바
- [x] 업그레이드 버튼
- [x] 닫기 기능

### 선택사항
- [ ] pg_cron 활성화
- [ ] Cron Job 등록
- [ ] 이메일 알림
- [ ] 슬랙 알림
- [ ] 관리자 대시보드

---

**작성일**: 2025-12-25
**버전**: 1.0
**상태**: 구현 완료, 테스트 준비
